# 🔒 PRODUCTION ENVIRONMENT VARIABLES
# Copy this file to your production environment and fill in the actual values
# NEVER commit the actual .env.production file with real secrets

# 🌐 NextAuth Configuration
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your-32-character-secret-here

# 🏗️ Sanity Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-08-24
SANITY_WRITE_TOKEN=your-write-token-here

# 🔐 Security
CSRF_SECRET=your-csrf-secret-here

# 💳 Payment (if using Razorpay)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-secret

# 📊 Analytics (optional)
GOOGLE_ANALYTICS_ID=your-ga-id
GOOGLE_TAG_MANAGER_ID=your-gtm-id

# 📧 Email (optional)
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# 🚀 Performance (AUTOMATIC - don't set manually)
# NODE_ENV=production  # ← Next.js sets this automatically
NEXT_TELEMETRY_DISABLED=1

# 📝 IMPORTANT NOTES:
# 1. Replace all 'your-*' values with actual production values
# 2. NEXTAUTH_URL must match your production domain exactly
# 3. NEXTAUTH_SECRET should be 32+ characters long
# 4. SANITY_WRITE_TOKEN must have write permissions
# 5. Never commit this file with real secrets
# 6. Use your hosting platform's environment variable system
# 7. DON'T set NODE_ENV - Next.js sets it automatically
# 8. NODE_ENV will be 'production' when deployed
